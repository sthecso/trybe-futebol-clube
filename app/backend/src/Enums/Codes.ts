enum Codes {
  notFound = 404,
  badRequest = 400,
  unauthorized = 401,
  forbidden = 403,
  conflict = 409,
  internalServerError = 500,
  serviceUnavailable = 503,
  OK = 200,
}

export default Codes;
