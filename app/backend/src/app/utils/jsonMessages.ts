const jsonMessages = {
  fieldsFilled: { message: 'All fields must be filled' },
  notFoundToken: { error: 'Token not found' },
  invalidToken: { error: 'Invalid token' },
};

export default jsonMessages;
